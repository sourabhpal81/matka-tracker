// Matka Tracker — app configuration.
//
// Leave FEED_URL unset for local preview / offline (the app uses the bundled
// data in feed.js). After you set up hosting (Firebase or GitHub), point this at
// your live feed so the app auto-updates daily. It MUST be an https URL.
//
// Example (GitHub raw):
// window.FEED_URL = "https://raw.githubusercontent.com/YOURNAME/matka-feed/main/feed.json";
//
// Example (Firebase Hosting):
// window.FEED_URL = "https://matka-tracker.web.app/feed.json";

// window.FEED_URL = "https://.../feed.json";
